{
  "name": "portfolio-niek-nijland",
  "description": "Niek Nijlands portfolio",
  "private": true,
  "version": "1.0.0",
  "author": "Niek Nijland <ngnijland@gmail.com>",
  "scripts": {
    "bootstrap": "lerna exec -- yarn --frozen-lockfile",
    "lint": "lerna run lint",
    "start": "lerna run empty-cache && lerna run start --parallel",
    "type-check": "lerna run type-check"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
